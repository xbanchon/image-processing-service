[build]
builder = "NIXPACKS"
buildCommand = "export CGO_ENABLED=1 && go build -C cmd/app -o out"

[build.phases.setup]
nixPkgs = ["...", "libgcc", "vips"]

[build.phases.install]
dependsOn = ["setup"]
cmds = ["go mod download", "whereis gcc"]

